dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.0
  flutter_riverpod: ^2.5.1
  shared_core:
    path: ../shared_core
  shared_ui:
    path: ../shared_ui
  flutter_dotenv: ^5.1.0
  webview_flutter: ^4.8.0

  # NEU:
  app_links: ^3.5.1  # (oder aktuelle stabile Version)

# Alte Zeile entfernen:
#  uni_links: ^0.5.1
