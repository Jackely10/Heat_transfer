name: app_consumer
description: GlovoLite Consumer app
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.0
  flutter_riverpod: ^2.5.1
  shared_core:
    path: ../shared_core
  shared_ui:
    path: ../shared_ui
  flutter_dotenv: ^5.1.0
  webview_flutter: ^4.8.0
  app_links: ^3.5.1   # ersetzt uni_links

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
